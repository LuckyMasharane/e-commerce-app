export class User {
    // firstName: string;
    // lastName: string;
    // email: string;
    // password: string
    constructor(public firstName, public lastName, public email, public password?){}
}